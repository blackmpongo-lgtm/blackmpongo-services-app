name: blackmpongo_services_app

description: A new Flutter project.

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  cloud_firestore: ^4.0.0
  firebase_auth: ^5.0.0
  firebase_storage: ^11.0.0
  stripe_flutter: ^6.1.0
  agora_rtc_engine: ^5.0.0
  flutter_riverpod: ^2.0.0
  http: ^0.15.0
  url_launcher: ^6.0.0
  shared_preferences: ^2.1.0
  hive: ^2.0.0
  intl: ^0.17.0
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
